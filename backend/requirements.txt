# ====================================================================
# K9 Training Vision System - Backend Dependencies (Phase 0)
# ====================================================================
# Python 3.12 + PyTorch 2.11+cu128 baseline
# PyTorch 不在此处指定版本，由 Phase 0 启动时按 ADR 0002 安装：
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

# === Web 框架 ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# === Pydantic / 配置 ===
pydantic==2.10.4
pydantic-settings==2.7.1

# === 数据库 ===
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.5.0

# === 任务队列 ===
celery[redis]==5.4.0
redis==5.2.1
flower==2.0.1

# === ML / CV（Phase 0 仅安装轻量，YOLO26 等大包留到 Phase 1） ===
numpy==2.2.1
pillow==11.0.0
opencv-python-headless==4.10.0.84
ultralytics==8.4.107

# === 工具 ===
python-dotenv==1.0.1
loguru==0.7.3
httpx==0.28.1
python-dateutil==2.9.0.post0
tqdm==4.67.1

# === 测试 ===
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# === 报告生成 ===
reportlab==4.2.5

# === 文档（FastAPI 自动文档） ===
# 已包含在 fastapi 中

# ====================================================================
# Phase 1 MVP 依赖（2026-07-26 追加，依据 ADR 0003 + RESEARCH_PHASE1_STACK_DEEP.md）
# ====================================================================

# === ONNX 推理（TensorRT 回退方案，Phase 1.1） ===
onnxruntime-gpu==1.20.1
onnx==1.22.0
onnxslim==0.1.94

# === 关键点处理 + PoseC3D 数据格式（Phase 1.2 / 1.3） ===
scipy==1.15.0
pandas==2.2.3

# === 视频处理（Phase 1.0 视频解码/抽帧） ===
# opencv-python-headless 已在 Phase 0 安装

# === PDF 报告图片嵌入（Phase 1.4） ===
matplotlib==3.10.0

# === 工作犬数据标注导出处理（Phase 1.3 Roboflow YOLO Pose 格式） ===
PyYAML==6.0.2

# ====================================================================
# 以下依赖不通过 pip 安装，仅作记录（按 RESEARCH_PHASE1_STACK_DEEP.md §1/§3）
# ====================================================================
# PyTorch 2.11+cu128: 见文件头注释（ADR 0002）
# TensorRT 10.8.0.6: 通过 NVIDIA 官方 ZIP 安装 + 手动 whl
#   C:\TensorRT-10.8.0.6\python\tensorrt-10.8.0.6-cp312-cp312-win_amd64.whl
# mmcv 2.1.0: 源码编译（仅 Phase 1.3 可选）
#   set MMCV_WITH_OPS=1 & set TORCH_CUDA_ARCH_LIST=12.0
#   pip install setuptools==79.0.1
#   python setup.py develop
# mmaction2: 源码安装（仅 Phase 1.3 可选）
#   git clone https://github.com/open-mmlab/mmaction2.git
#   pip install -v -e .
