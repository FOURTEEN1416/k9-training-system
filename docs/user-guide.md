# 用户手册（训导员使用指南）

> 工作犬训练机器视觉识别系统 — Phase 1 MVP
> 适用版本: v0.1.0
> 目标用户: 工作犬训导员 / 犬只管理人员 / 系统管理员
> 访问地址: http://127.0.0.1:5173（前端） / http://127.0.0.1:8000/docs（API 文档）

## 1. 系统概述

### 1.1 系统用途

本系统基于机器视觉技术，对工作犬训练视频进行自动分析，输出客观的评分报告。当前支持两类场景：

| 场景 | 中文名 | 适用阶段 | 评分维度 | 输出 |
|------|--------|---------|---------|------|
| `obedience_trial` | 科目测评 | 训练中/考核 | 5 维（准确度/延迟/保持/注意力/步态） | PDF 评分报告 |
| `puppy_selection` | 幼犬选育 | 幼犬筛选 | 3 维（食物欲望/猎物欲望/胆量） | PDF 评分报告 |

### 1.2 核心能力

- **24 关键点姿态检测**：基于 YOLO26-pose 微调模型，识别犬只全身 24 个关键点
- **8 类基础行为识别**（科目测评）：坐 / 卧 / 立 / 随行 / 坐立 / 停留 / 叫 / 咬
- **物体检测**（选育场景）：球类 / 食物 / 玩具 / 人 / 犬
- **9 信号提取**（选育场景）：接近延迟 / 接近速度 / 嗅闻时长 / 追逐延迟 / 追逐速度 / 衔持时长 / 退避距离 / 僵直时长 / 恢复时间
- **YAML 评分卡**：训导员可修改评分规则，热加载生效，无需重启
- **PDF 报告**：自动生成评分报告，可下载/打印

### 1.3 系统架构（简化）

```
[训导员浏览器] ←→ [前端 Vue] ←→ [FastAPI 后端]
                                      ↓
                              [Celery Worker]
                                  ↓     ↓
                          [YOLO26 推理] [评分引擎]
                                  ↓
                          [PDF 报告生成]
```

## 2. 访问系统

### 2.1 首次访问

1. 确认系统已部署（参见 `docs/deployment.md`）
2. 打开浏览器，访问 http://127.0.0.1:5173
3. 默认无需登录（Phase 1 MVP 未实现权限系统）

### 2.2 浏览器要求

- Chrome / Edge 110+（推荐）
- Firefox 110+
- 分辨率 ≥ 1366×768

## 3. 主界面导航

系统分为 4 个主页面，通过左侧导航栏切换：

| 菜单 | 页面 | 功能 |
|------|------|------|
| 上传视频 | UploadView | 上传训练视频，启动自动分析 |
| 历史记录 | HistoryView | 查看所有视频处理状态与评分 |
| 报告详情 | ReportView | 查看单个视频的详细评分报告 + PDF |
| 系统管理 | AdminView | 管理模型 / 评分卡 / 犬只档案 |

## 4. 上传视频（核心流程）

### 4.1 准备视频

**格式要求**：
- 文件格式：.mp4 / .avi / .mov / .mkv / .webm
- 文件大小：≤ 2 GB
- 视频时长：建议 10 秒 - 5 分钟
- 分辨率：建议 480p 及以上
- 帧率：建议 25-60 FPS

**拍摄建议**：
- 单犬场景为主（多犬追踪 Phase 3 支持）
- 光线充足，避免逆光
- 摄像机固定或缓慢移动
- 犬只全身入镜，无遮挡

### 4.2 上传步骤

1. 点击左侧菜单 **"上传视频"**
2. **选择场景**：
   - `科目测评`：训练中/考核场景（坐/卧/立/随行等指令行为）
   - `幼犬选育`：幼犬筛选场景（食物/玩具/胆量测试）
3. **选择犬只**（可选）：从下拉框选择已录入的犬只档案；若无则选择"未关联"
4. **拖拽视频** 到上传区域，或点击区域选择文件
5. 确认信息无误后，点击 **"开始上传"**
6. 上传完成后，页面自动跳转到 **报告详情页**

### 4.3 处理状态

上传后系统进入异步处理，状态变化：

| 状态 | 含义 | 预期耗时 |
|------|------|---------|
| `pending` | 已入库，等待 Worker 接收 | < 5 秒 |
| `processing` | 推理中（姿态/行为/物体/评分） | ≤ 1 分钟/分钟视频 |
| `completed` | 处理完成，PDF 已生成 | - |
| `failed` | 处理失败，查看错误信息 | - |

**实时轮询**：报告页每 3 秒自动刷新状态，无需手动刷新。

## 5. 查看报告

### 5.1 报告详情页

处理完成后，报告页显示：

- **视频信息**：文件名 / 时长 / 帧率 / 分辨率 / 上传时间
- **犬只信息**（如已关联）：名字 / 品种 / 性别 / 芯片号 / 训练阶段
- **评分总览**：
  - 总分（0-100）
  - 判定结果：`合格` / `基本合格` / `不合格`
  - 各维度分数进度条
- **PDF 预览**：内嵌浏览器 PDF 预览，可下载/打印

### 5.2 评分维度说明

#### 科目测评（5 维）

| 维度 | 权重 | 信号来源 | 含义 |
|------|------|---------|------|
| 准确度 | 0.30 | `action_correct / action_count` | 指令执行正确率 |
| 延迟 | 0.20 | `command_to_action_latency` | 从指令到行为响应的延迟 |
| 保持 | 0.20 | `action_duration` | 行为持续时间 |
| 注意力 | 0.15 | `focus_ratio` | 犬只注意力集中度 |
| 步态 | 0.15 | `gait_score` | 随行步态质量 |

#### 幼犬选育（3 维）

| 维度 | 权重 | 信号来源 | 含义 |
|------|------|---------|------|
| 食物欲望 | 0.40 | `approach_latency` / `approach_speed` / `sniff_duration` | 接近食物的速度与嗅闻时长 |
| 猎物欲望 | 0.40 | `chase_latency` / `chase_speed` / `hold_duration` | 追逐玩具/球的速度与衔持时长 |
| 胆量 | 0.20 | `retreat_distance` / `recovery_time` / `freeze_duration` | 面对陌生刺激的退避距离与恢复时间 |

### 5.3 下载 PDF

- 报告页右上角 **"下载 PDF"** 按钮
- 或历史记录页对应视频行的 **下载** 图标
- PDF 文件名格式：`{video_id}.pdf`
- 存储路径：`reports/{video_id}.pdf`

## 6. 历史记录

### 6.1 列表视图

历史页显示所有上传的视频，支持：

- **统计卡片**：总视频数 / 已完成 / 处理中 / 失败
- **筛选**：场景 / 状态 / 犬只
- **排序**：按上传时间倒序（默认）
- **操作**：查看报告 / 下载 PDF / 删除

### 6.2 状态图标

| 图标 | 状态 |
|------|------|
| ⏳ | pending / processing |
| ✅ | completed |
| ❌ | failed |

## 7. 系统管理（管理员）

系统管理页分为 4 个 Tab：

### 7.1 模型管理

- **查看模型列表**：显示所有已注册的 ML 模型版本
- **注册新模型**：点击"注册模型"按钮，填写：
  - 模型名称（如 `yolo26n-pose-v1`）
  - 版本号（如 `1.0.0`）
  - 文件路径（如 `runs/train-2/weights/best.onnx`）
  - 模型类型（pose / detection / behavior）
  - 评估指标（mAP50 / mAP50-95）
- **设为当前生产模型**：选择某个模型版本激活为生产用

### 7.2 评分卡配置（YAML 编辑器）

**核心功能**：在线编辑评分卡 YAML，修改后热加载生效。

#### 操作步骤

1. 选择场景（`puppy_selection` 或 `obedience_trial`）
2. YAML 编辑器显示当前评分卡内容
3. 修改规则 / 权重 / 阈值
4. 点击 **"保存"** → 系统校验 Schema → 写入文件 → 热加载
5. 用 **"试评分"** 功能验证修改效果

#### YAML 结构示例（选育评分卡）

```yaml
scoring_engine:
  name: "幼犬选育评分卡"
  version: "1.1.0"
  scene: "puppy_selection"
  dimensions:
    - id: food_drive
      name: "食物欲望"
      weight: 0.40  # 权重，所有维度之和必须 = 1.0
      rules:
        - id: food_high
          condition: "approach_latency < 1.0 and approach_speed > 2.0 and sniff_duration > 1.0"
          score: 90
          label: "高"
        # ... 更多规则
      default:      # 所有规则未命中时的默认分
        score: 40
        label: "中低"
  thresholds:
    pass: 70        # 合格线
    borderline: 60  # 基本合格线
    fail: 0         # 淘汰线
```

#### 修改建议

- **调整权重**：所有维度 `weight` 之和必须 = 1.0，否则 Schema 校验失败
- **修改阈值**：`pass` ≥ `borderline` ≥ `fail`
- **新增规则**：`id` 必须唯一；`condition` 表达式语法见下
- **规则顺序**：按顺序匹配，首个命中生效（`True` 作为兜底放最后）

#### 条件表达式语法

| 操作符 | 含义 | 示例 |
|--------|------|------|
| `<` `>` `<=` `>=` `==` `!=` | 比较 | `approach_latency < 1.0` |
| `and` `or` | 逻辑与/或 | `a < 1 and b > 2` |
| `not` | 逻辑非 | `not (a > 5)` |
| `True` | 永真（兜底） | `condition: "True"` |

信号变量名见 §5.2 信号来源列。

#### 试评分

修改评分卡后，可输入一组信号值，立即查看评分结果，验证规则是否符合预期。

### 7.3 犬只档案管理

- **新增犬只**：点击"新增犬只"按钮，填写：
  - 名字（必填）
  - 品种（如 GermanShepherd / BelgianMalinois / Labrador）
  - 性别（公 / 母）
  - 芯片号（15 位数字）
  - 出生日期
  - 训练阶段（P0 / P1 / P2 / P3）
  - 训导员（可选）
  - 备注
- **编辑 / 删除**：列表行操作
- **搜索**：按名字 / 芯片号筛选

### 7.4 系统信息

显示系统运行状态：
- 后端版本 / 环境
- 数据库连接状态
- Celery worker 状态
- 模型加载状态
- 评分卡版本

## 8. 典型工作流

### 8.1 训导员日常使用

```
1. 拍摄训练视频（手机/摄像机）
2. 登录系统 → "上传视频"
3. 选择场景（科目测评）→ 选择犬只 → 拖入视频
4. 等待处理完成（约 1 分钟/分钟视频）
5. 查看报告 → 7 维评分 + PDF
6. 下载 PDF 归档 / 打印汇报
```

### 8.2 选育评估工作流

```
1. 准备食物 / 玩具 / 陌生刺激物
2. 拍摄幼犬反应视频（建议 1-3 分钟）
3. 上传视频 → 选择场景（幼犬选育）
4. 系统自动检测物体 + 提取 9 信号
5. 查看 3 维评分（食物欲望/猎物欲望/胆量）
6. 根据评分决定是否进入训练体系
```

### 8.3 评分规则调整工作流

```
1. "系统管理" → "评分卡配置" Tab
2. 选择目标场景
3. 修改 YAML（如调整权重、新增规则）
4. 点击"保存" → Schema 校验通过
5. 用"试评分"验证规则效果
6. 上传新视频 → 新评分卡生效
```

## 9. 常见问题

### Q1: 上传后状态长时间停在 "processing"

**A**: 
- 检查 Celery worker 是否运行（系统管理 → 系统信息）
- 视频时长越长处理越久，预期 ≤ 1 分钟/分钟视频
- 首次上传有冷启动开销（约 10-15 秒加载模型）
- 若超过 5 分钟仍未完成，查看后端日志排查

### Q2: 评分为 0 或所有维度都 "未评估"

**A**: 
- 视频中犬只可能未检出（光线不足 / 遮挡 / 拍摄角度不佳）
- 信号值未触发任何规则，使用默认分
- 在"系统管理 → 评分卡配置"中检查规则条件是否过严

### Q3: PDF 报告无法下载

**A**:
- 检查 `reports/` 目录写入权限
- 浏览器拦截弹窗，允许该站点下载
- 后端日志查看 reportlab 报错

### Q4: 修改评分卡后评分没变化

**A**:
- 确认点击"保存"成功（应无红色错误提示）
- 热加载机制基于文件 mtime 检测，约 1 秒延迟
- 用"试评分"功能确认新规则生效
- 若用相同视频测试，需重新上传（旧评分结果不会自动更新）

### Q5: 视频上传失败 "413 Request Entity Too Large"

**A**:
- 文件超过 2 GB 限制
- 使用视频剪辑工具分割为多段
- 或修改 `backend/app/core/config.py` 中 `upload_max_size_mb`

### Q6: 如何修改评分卡中某个维度的权重？

**A**:
1. 系统管理 → 评分卡配置 → 选择场景
2. 找到目标维度（如 `food_drive`）
3. 修改 `weight` 值（0-1 之间小数）
4. **同步调整其他维度权重，确保总和 = 1.0**（否则保存失败）
5. 保存 → 试评分验证

### Q7: 系统支持哪些犬种？

**A**: Phase 1 MVP 使用 Dog-Pose 数据集微调，对常见工作犬种（德牧 / 马犬 / 拉布拉多 / 史宾格等）均有效。对非常见犬种（如玩具犬）关键点检测精度可能下降。

### Q8: 数据安全性如何保证？

**A**:
- Phase 1-3 严格本地化部署，无云依赖
- 数据不出本机（适用于公安/海关场景）
- 视频文件存储在 `data/uploads/`，PDF 报告在 `reports/`
- 数据库密码在 `.env` 中配置，不入 Git

## 10. 术语表

| 术语 | 含义 |
|------|------|
| 关键点 | 犬只身体上的特征点（鼻、眼、耳、肩、肘、腕、髋、膝、踝等），共 24 个 |
| 行为 episode | 一次完整的行为事件（如"坐下"从起拍到结束），含起止帧 + 置信度 |
| 信号 | 评分引擎的输入变量（如 `approach_latency` 接近延迟） |
| 评分卡 | YAML 配置文件，定义评分维度 / 规则 / 阈值 |
| 热加载 | 修改 YAML 后无需重启服务即生效 |
| 场景 | 评分场景类型（科目测评 / 幼犬选育） |
| Celery Worker | 后台异步任务执行进程 |
| ONNX Runtime | GPU 推理后端（Phase 1 生产推荐） |

## 11. 反馈与支持

- **Bug 报告**：记录复现步骤 + 截图 + 后端日志，提交给系统管理员
- **评分规则建议**：基于实际训练经验提出，由 ML 开发评估纳入评分卡
- **功能需求**：Phase 2 路线图见 `dev-docs/stages/phase-1.md`（待规划）

## 12. 附录

### 12.1 API 速查

| 接口 | 方法 | 用途 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/dogs` | GET/POST | 犬只列表/新增 |
| `/api/dogs/{id}` | GET/PUT/DELETE | 犬只详情/编辑/删除 |
| `/api/videos/upload` | POST | 上传视频 |
| `/api/videos/{id}/status` | GET | 查询处理状态 |
| `/api/videos/{id}/report` | GET | 下载 PDF 报告 |
| `/api/scores?video_id=` | GET | 查询评分记录 |
| `/api/scoring/configs` | GET | 列出所有评分卡 |
| `/api/scoring/configs/{scene}` | GET/PUT | 获取/更新评分卡 |
| `/api/scoring/evaluate` | POST | 试评分 |
| `/api/models` | GET | 模型列表 |
| `/api/models/register` | POST | 注册新模型 |
| `/api/models/current` | GET | 当前生产模型 |

完整 OpenAPI 文档：http://127.0.0.1:8000/docs

### 12.2 视频拍摄检查清单

- [ ] 单犬场景（无其他犬只干扰）
- [ ] 犬只全身入镜
- [ ] 光线充足，顺光拍摄
- [ ] 摄像机固定或平稳移动
- [ ] 无遮挡物
- [ ] 时长 10 秒 - 5 分钟
- [ ] 分辨率 ≥ 480p
- [ ] 帧率 25-60 FPS
- [ ] 格式 .mp4（推荐）
